# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/maitri_db
spring.datasource.username=postgres
spring.datasource.password=Chann30@
spring.datasource.driver-class-name=org.postgresql.Driver

# ===========================================
# JPA / HIBERNATE CONFIGURATION
# ===========================================
# Hibernate DDL auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Server port (default 8080)
server.port=8080

# ===========================================
# JWT CONFIGURATION (Custom Properties)
# ===========================================
# JWT Secret Key (Change this in production!)
app.jwt.secret=maitriSecretKeyForWomensHealthPlatform2024SuperSecureSecretKeyForJWTTokenGeneration256Bits
# JWT Token expiration time (24 hours = 86400000 milliseconds)
app.jwt.expiration=86400000

# ===========================================
# AI CONFIGURATION
# ===========================================
# Google Gemini API Key (Get from https://makersuite.google.com/app/apikey)
app.gemini.api-key=AIzaSyDAcq3CJ129f8w240dTKXV4xnblWRhFLps
app.gemini.model=gemini-1.5-flash

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
# Log levels
logging.level.com.maitri=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
# Application name
spring.application.name=maitri-backend

# Profile (development)
spring.profiles.active=dev

# ===========================================
# VALIDATION CONFIGURATION
# ===========================================
# Enable validation
spring.jpa.properties.hibernate.validator.apply_to_ddl=false
